{{#if returns}}
{{#if returns.[0].description}}  

*Returns*: {{#each returns~}}
  {{#if type~}}
  {{#if type.names}}{{>linked-type-list types=type.names delimiter=" v " ~}}{{/if}}
  {{~#if description}} - {{{formatDescription (inlineLinks description)}}}{{/if~}}
  {{else~}}
  {{{formatDescription (inlineLinks description)}~}}
  {{/if~}}
{{~/each}}
  
{{/if}}{{/if}}